# Project documents are hand-wrapped to ~88 columns and their tables are aligned
# for reading in a terminal or a narrow editor pane. Prettier pads every table
# cell to the width of its longest entry, which pushes these well past 200
# columns and makes them materially harder to read.
#
# These files are read far more often than they are edited, and by people rather
# than tools, so readability wins over mechanical consistency.
*.md

# Generated or vendored — not ours to format.
pnpm-lock.yaml
apps/api/prisma/migrations/
apps/web/.next/
**/dist/
**/node_modules/
